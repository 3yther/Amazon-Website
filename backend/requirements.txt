# Python 3.11. Django 5.2 is the LTS release (supported to April 2028) and the
# newest line that runs on 3.11; Django 6 needs Python 3.12+.
Django==5.2.17
djangorestframework==3.18.1
django-cors-headers==4.9.0
python-dotenv==1.2.3
# The AI assistant (chatbot app) calls Anthropic's API. Only the chatbot
# imports it, so the rest of the site runs without it installed.
anthropic==1.8.0

# Pulled in by Django, pinned so every machine installs the same thing.
asgiref==3.12.1
sqlparse==0.6.0

# Deployment (Railway preview now, AWS later). Not needed for local development,
# which uses SQLite and the Django dev server.
gunicorn==26.2.0                 # production app server; runserver is for development only
psycopg[binary]==3.3.6           # PostgreSQL driver, used when DATABASE_URL is set
typing-extensions==4.16.0        # pulled in by psycopg on Python 3.11, pinned like the above

# Production additions still to come (AWS):
# django-storages[s3]  store ContentItem files in S3
